lang: es

# Incluir apenas o conteúdo em espanhol
include:
  - es
  - swagger-ui

# Excluir os demais idiomas e o redirecionamento
exclude:
  - pt-br
  - en
  - index.html

# Permalinks simples
defaults:
  - scope:
      path: "es"
      type: "pages"
    values:
      layout: default
      